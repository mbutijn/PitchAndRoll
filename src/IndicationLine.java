public class IndicationLine {
    protected int length;
    protected double distance; // 20 pixel per degree

    IndicationLine(int length, double distance){
        this.length = length;
        this.distance = distance;
    }
}
